# Uncomment the next line to define a global platform for your project
platform :macos, '10.11'

target 'dynamicd' do
  # Pods for dynamicd
  pod 'SocketRocket'
  pod 'Sparkle',  '~> 2.0.0'  

  target 'dynamicdTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
